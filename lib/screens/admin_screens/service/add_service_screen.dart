import 'package:flutter/material.dart';

class AddServiceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Service'),
      ),
      body: Center(
        // Add your form or UI elements for adding a service here
        child: Text('Add Service Form Goes Here'),
      ),
    );
  }
}
