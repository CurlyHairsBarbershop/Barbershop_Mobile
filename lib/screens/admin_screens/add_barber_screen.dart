import 'package:flutter/material.dart';

class AddBarberScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Barber'),
      ),
      body: Center(
        // Add your form or UI elements for adding a barber here
        child: Text('Add Barber Form Goes Here'),
      ),
    );
  }
}
